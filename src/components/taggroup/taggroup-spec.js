import React from 'react';
import { mount, shallow } from 'enzyme';
import Tag from './tag';

describe('Tag', () => {
  it('should render the component', () => {
  });

  it('should be clickable', () => {
  });

  it('should disappear when icon is clicked', () => {
  });
});

